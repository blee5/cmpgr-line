#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "image.h"
#include "imageio.h"
#include "draw.h"
#include "matrix.h"

int main()
{
    /* struct matrix *m = new_matrix(1, 1); */
    /* setval_matrix(m, 9, 3, 12); */
    /* ident(m); */
    /* print_matrix(m); */
    /* free_matrix(m); */
    return 0;
}
